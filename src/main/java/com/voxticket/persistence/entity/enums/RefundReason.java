package com.voxticket.persistence.entity.enums;

/** NOT specified verbatim in the spec. Mirrors the flows that actually create a Refund in this system. */
public enum RefundReason { CANCELLATION, RETURN, CLAIM, GOODWILL, OTHER }