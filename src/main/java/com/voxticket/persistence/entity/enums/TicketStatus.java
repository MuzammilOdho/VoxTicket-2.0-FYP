package com.voxticket.persistence.entity.enums;

/** NOT specified verbatim in the spec. Default lifecycle for manual-work tickets (spec §34). */
public enum TicketStatus { OPEN, IN_PROGRESS, RESOLVED, CLOSED }