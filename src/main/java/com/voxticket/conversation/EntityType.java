package com.voxticket.conversation;

/** Spec §16. */
public enum EntityType {
    ORDER_REFERENCE,
    RETURN_REFERENCE,
    REFUND_REFERENCE,
    TICKET_REFERENCE,
    PRODUCT,
    PAYMENT_REFERENCE,
    AMOUNT,
    DATE
}